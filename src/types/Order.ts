export type Order = {
  id: number;
  userId: number;
  productId?:number;
  productIds?: number[];
};

// export type Orderb = {
//   id: number;
//   userId: number;
//   productIds?: { id: number }[];
// };
